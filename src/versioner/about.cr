# `Versioner` is a basic utility to help keep a repo's version in sync and updated across various files
require 'reader.cr'
require 'writer.cr'

module Versioner
  # module About
  #   VERSION = "0.0.3"
  # end
end
